I'm an abstract class which represents the main application component.


To be used, an subclass should be created and this methods should be overwritten:

    Instance side
	applicationClasses
	menuEntries
	storeInitialization
	title

    Class side:
	applicationName
