I represent Tests for persistance with PostgreSQL 

    Instance Variables
	app:		<steam main component>
	article:		<STArticle example>
	game:		<STGame example>
	gameCat:		<STCategory example>
	store:		<DB connection>
